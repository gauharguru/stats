{
  "name": "ahs-student-fee-management",
  "version": "1.0.0",
  "private": true,
  "description": "AHS Nursing College - Student Fee, Admission & Financial Management System",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "db:setup": "npm --prefix server run db:setup",
    "db:demo": "npm --prefix server run db:demo",
    "dev:api": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web run build && npm --prefix server run build",
    "start": "npm --prefix server start",
    "test": "npm --prefix server test",
    "typecheck": "npm --prefix server run typecheck && npm --prefix web run typecheck"
  }
}
